<p id="notice"><%= notice %></p>

<h1>Solicitudes</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Servicio</th>
      <th>Creada por</th>
      <th>Prioridad</th>
      <th>Estado</th>
      <th>Supervisor acargo</th>
      <th>Bodega</th>
      <th colspan="3">Opciones</th>
    </tr>
  </thead>

  <tbody>
    <% @solicituds = Solicitud.accessible_by(current_ability)
    @solicituds.each do |solicitud| %>
      <tr>
        <td><%= solicitud.nombre %></td>
        <td><%= solicitud.usuario.nombre %></td>
        <td> </td>
        <td> </td>
        <td> </td>
        <td> </td>
        <td><%= link_to 'Ver', solicitud %></td>
        <td><%= link_to 'Editar', edit_solicitud_path(solicitud) %></td>
        <td><%= link_to 'Eliminar', solicitud, method: :delete, data: { confirm: '¿Esta seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Crear Solicitud', new_solicitud_path %>
